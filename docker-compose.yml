

services:
  backend:
    build:
      context: .                   # Root of your project
      dockerfile: backend.dockerfile  # Multi-stage Dockerfile for frontend + backend
    ports:
      - "8080:8080"                # Maps container port 8080 to host port 8080
    environment:
      - SPRING_PROFILES_ACTIVE=prod  # Example environment variable for Spring Boot
